{
	"name": "htrncontrol-server",
	"displayName": "HTR NControl Server",
	"version": "0.1.0",
	"description": "HTTP/WebSocket API server for remote controlling browser tabs via the HTR NControl extension",
	"type": "module",
	"license": "MIT",
	"scripts": {
		"dev": "bun run --watch index.ts",
		"start": "bun run index.ts"
	},
	"devDependencies": {
		"@types/bun": "^1.3.7"
	}
}
