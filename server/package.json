{
	"name": "how-to-recorder-server",
	"displayName": "How-To Recorder Server",
	"version": "0.1.0",
	"description": "HTTP/WebSocket API server for remote controlling browser tabs via the How-To Recorder extension",
	"type": "module",
	"license": "MIT",
	"scripts": {
		"dev": "bun run --watch index.ts",
		"start": "bun run index.ts"
	},
	"dependencies": {
		"ws": "^8.16.0"
	},
	"devDependencies": {
		"@types/ws": "^8.5.10",
		"@types/bun": "^1.3.7"
	}
}
